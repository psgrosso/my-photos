package model.comparator;

import model.photo.element.PhotoCollection;
import model.photo.identifier.PhotoIdentifier;

/**
 * Created by pablo on 3/6/16.
 */
public class PhotoComparator {
    public PhotoComparator(PhotoCollection sourceCollection, PhotoCollection targetCollection) {

    }

    public void compare(PhotoIdentifier rootPhotoElement) {

    }
}
